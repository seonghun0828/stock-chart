# AGENTS

## Purpose

LS증권 모의투자 API 기반의 6개 섹터 실시간 마켓중심 카피 페이지를 구현하는 저장소다.

## Source of Truth

- 요구사항과 설계 기준은 `docs/superpowers/specs/2026-05-31-market-center-design.md`를 따른다.
- 화면 구조는 제공된 예시 PNG를 최대한 가깝게 유지한다.
- 기능이 없는 UI는 임의로 추가하지 않는다.

## Architecture Rules

- 프론트엔드는 React
- 백엔드는 Node.js + Express
- 프론트와 백엔드 통신:
  - REST: 최초 렌더용 마켓 스냅샷, 배포/연결 상태 확인
  - WebSocket: 연결 직후 상태 동기화, 이후 실시간 시세 반영
- 정렬과 집계 계산은 항상 백엔드가 담당한다.
- 프론트는 백엔드 응답 순서를 그대로 렌더링하는 얇은 클라이언트로 유지한다.

## Market Rules

- 섹터는 6개만 사용한다:
  - 반도체
  - 조선
  - 방산
  - 바이오
  - 전력기기
  - 금융
- 섹터별 종목군은 백엔드 설정 파일의 `sector -> 종목코드 배열`로 관리한다.
- 카드 노출 종목 수는 상수로 관리한다. 초기값은 `4`
- 섹터 정렬 기준:
  - 각 섹터 상승률 상위 3개 종목 평균
  - 내림차순 정렬
- 섹터 내부 종목도 상승률 내림차순 정렬한다.
- 카드 헤더 오른쪽 값은 현재 노출된 종목들의 거래대금 합계다.

## LS OpenAPI Rules

- 모의투자 환경 기준으로 구현한다.
- LS증권 인증키는 절대 커밋하지 않는다.
- 초기 데이터 보강은 REST로 처리해 첫 화면과 정렬 기준을 채운다.
- 장중 업데이트는 LS증권 WebSocket 수신값으로만 처리한다.
- WebSocket 틱마다 LS증권 REST를 다시 호출하지 않는다.
- WebSocket 업데이트는 전체 목록 교체가 아니라 종목 단위 patch로 처리한다.
- 이미 메모리에 있는 종목은 새 이벤트가 없어도 기존 값을 유지한다.
- WebSocket 재연결 직후 상태 보정이 필요할 때만 REST를 1회 재조회할 수 있다.
- 서버는 최신 종목 상태를 메모리에 유지하고 그 상태로 재정렬한다.

## UI Rules

- 검색 기능은 구현하지 않는다.
- 상단은 로고와 현재 시각 중심으로 구성한다.
- 본문은 6개 카드가 예시 PNG처럼 밀도 있게 보이는 반응형 그리드를 따른다.
- 카드 첫 줄 뉴스는 LS증권 응답에 값이 있을 때만 표시한다.
- 종목 행 하단의 미니 바는 일봉 스타일의 가격 범위 시각화로 구현한다.
- 하단 탭 스타일 영역은 정적 UI로 둘 수 있다.

## Delivery Rules

- 외부에서 접속 가능한 URL 제출을 전제로 한다.
- 프론트엔드와 백엔드 배포 설정은 분리 가능하게 유지한다.
- 환경변수 예시 파일을 제공하고 실제 비밀값은 로컬 또는 배포 환경변수로만 관리한다.
